---
name: Project issue progress
description: Which issues have been completed, what PRs exist, and what's in flight
type: project
---

Issue #5 (SVG stage + draggable character) — merged to main via PR #24.

Issue #6 (Ball path SVG rendering) — in progress, branch feat/issue-6-ball-path-rendering. All code written, tests passing (64), build clean. PR not yet created as of last session.

**Why:** User is working through a sequential issue list (#1 is the parent epic). Each issue is implemented via TDD with a PR per issue.

**How to apply:** When resuming, check if the branch exists and continue from where it left off. Next step after issue #6 would be issue #7 (check with `gh issue view 7`).
